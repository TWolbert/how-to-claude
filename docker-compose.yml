services:
  how-to-claude:
    build: .
    ports:
      - "9039:9039"
    environment:
      - DATABASE_URL=/data/local.db
      - APP_NAME=How to Claude
      - AUTH_REGISTER_ENABLED=true
    volumes:
      - how-to-claude-data:/data

volumes:
  how-to-claude-data:
